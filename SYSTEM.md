# 做图工作室系统

这个仓库不再只存单张海报，而是作为一个可持续运行的“做图工作室”。

## 核心原则

- **审美判断不自动化**：人物选图、最终构图、整体美感由人工/主设计者确认。
- **机械工作自动化**：下载、整理、去重、抠图、放大、格式转换、SVG/PNG 导出交给工具或 Agent。
- **中文文字矢量化**：标题、正文、名单优先保留为 SVG/HTML 文字，避免图像模型生成中文错字或发糊。
- **所有过程可追溯**：需求、素材、选图记录、源文件、成图全部留在 Git。
- **质量优先**：质量 > 稳定性 > 可控性 > 免费。低质量 Agent 即使免费也不接入正式流程。

## 标准流水线

1. `jobs/`：建立任务，保存文案、尺寸、风格目标、状态。
2. `素材/`：导入候选照片和原始素材。
3. `review/`：人工/主设计者选图，明确使用哪张图以及原因。
4. `work/`：生成抠图、放大图、背景处理图等中间素材。
5. `posters/`：保存 SVG/HTML 等可编辑海报源文件。
6. `outputs/`：导出 PNG/JPG/PDF 发布版。
7. `.github/workflows/`：自动执行格式检查、SVG→PNG、打包产物。

## Agent 分工

- 素材 Agent：下载、分类、去重、记录来源。
- 抠图 Agent：背景移除、边缘优化、透明 PNG 输出。
- 放大 Agent：必要时做人像超分辨率。
- 排版工具：SVG/HTML/CSS，保证中文清晰。
- 审美 Gate：不自动选最终照片，不自动发布；必须经过人工/主设计者确认。

## 目录约定

```text
zuotu/
├─ jobs/                 # 每张图的任务定义
├─ 素材/                 # 原始素材
├─ review/               # 选图与审美确认记录
├─ work/                 # 抠图/放大/中间文件
├─ posters/              # SVG/HTML 源文件
├─ outputs/              # 最终 PNG/JPG/PDF
├─ scripts/              # 处理脚本
├─ agents/               # Agent 说明/接口
└─ .github/workflows/    # 自动化
```

## 当前任务

`jobs/001-gettr-feedback/` 为第一张正式测试海报。当前阶段：等待真实人物照片进入 Git 后，由主设计者选图，再进入抠图和正式重排版。
